# ============================================================
# Traefik Static Configuration  —  DeployX
# ============================================================

api:
  insecure: true # dashboard at :8080 (dev convenience; disable in prod)
  dashboard: true

log:
  level: INFO

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: deployx-internal

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"
