# ============================================================
# DeployX Environment Configuration
# Copy this file:  cp .env.example .env
# The quick-start.sh script generates secure values automatically.
# ============================================================

# ---------- Database ----------
POSTGRES_DB=deployx
POSTGRES_USER=deployx_user
POSTGRES_PASSWORD=change_this_password_in_production

# ---------- Backend ----------
SECRET_KEY=your-super-secret-key-change-this-in-production-min-32-chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# ---------- Frontend ----------
# No NEXT_PUBLIC_API_URL needed — Next.js rewrites proxy /api/* to the backend internally.

# ---------- Cloudflare Tunnel (auto-populated after setup) ----------
TUNNEL_TOKEN=
